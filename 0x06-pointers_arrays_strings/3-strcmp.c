#include "main.h"

/**
 * _strcmp - function that compares two strings.
 * @s1:first string argument character
 * @s2: second string argument character
 *
 * Return: the compare values
 */
int _strcmp(char *s1, char *s2)
{
	strcmp(s1, s2);
	return (strcmp(s1, s2));
}
